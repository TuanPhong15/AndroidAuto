import {useCarNavigation} from 'react-native-android-auto';

export function Main() {
  const navigation = useCarNavigation();

  return (
    <list-template
      title={"GOFA SCREEN TEST"}
    >
    </list-template>
  );
}